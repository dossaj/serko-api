{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/Serko.Expense.Server/Serko.Expense.Server.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}